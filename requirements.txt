# Streamlit Community Cloud: select Python 3.12 in Advanced settings.
# Exact pins prevent uv/pip from backtracking through several large CV wheels.
streamlit==1.59.2
numpy==1.26.4
pandas==2.3.3
plotly==6.9.0
reportlab==4.5.1
protobuf==4.25.9

# MediaPipe depends on opencv-contrib-python; keep one cv2 provider. The camera
# engine uses mp.solutions, which was removed after MediaPipe 0.10.21.
opencv-contrib-python==4.11.0.86
mediapipe==0.10.21
streamlit-webrtc==0.75.1
av==15.1.0
