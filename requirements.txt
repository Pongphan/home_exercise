# Streamlit Community Cloud core build.
# Native camera dependencies live in requirements-camera.txt so a large
# MediaPipe/JAX/OpenCV build cannot prevent the tracker from deploying.
streamlit==1.59.2
pandas==2.3.3
plotly==6.9.0
reportlab==4.5.1
